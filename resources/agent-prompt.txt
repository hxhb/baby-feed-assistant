收到baby-feed webhook事件`{type}`（{timestamp}）请用`baby-feed-assistant`技能处理：技能内的`resources/webhook-analysis.md`是该事件的唯一权威playbook，CRITICAL01/02/03 三条铁律以及输出格式、各事件分册都在那里维护，请严格遵守。

⚠️ **第一步必须做**：用 **Write 工具**把下方 raw JSON 写到 `/tmp/bf-event-{id前缀}.json`，再用 `@<路径>` 调脚本（playbook CRITICAL03）。直接把 JSON 当 bash 参数会被 host 安全扫描器拒绝。

事件数据：
```json
{__raw__}
```
